# gitlab-ci

stages:
  - build
  - deploy

before_script:
  - curl -s toast.sh/install-v3 | bash

build:
  stage: build
  image: php:5.6
  script:
    - ~/toaster/toast.sh prepare
    - ~/toaster/toast.sh build php
  artifacts:
    paths:
      - target/

deploy:
  stage: deploy
  image: python:latest
  script:
    - ~/toaster/toast.sh install aws
    - ~/toaster/toast.sh releases bucket public

after_script:
  - ls -alh target
